'use strict';

//import './Polyfill.js';
//import Ajax_action from './modules/Ajax_action.js';

var ajax_action_btns = [];

function init() {
  console.log('ok so far');
  // Array.from(document.querySelectorAll('.ajax_action')).forEach($elm => {
  //   ajax_action_btns.push(new Ajax_action($elm));
  // });
}

document.addEventListener('DOMContentLoaded', function () {
  init();
});